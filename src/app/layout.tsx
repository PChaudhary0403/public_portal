import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Providers } from "./providers"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "JanSeva - Public Grievance Portal",
  description: "Smart Public Grievance & Authority Accountability Platform. Register complaints, track progress, and ensure accountability for public services.",
  keywords: ["grievance", "complaint", "government", "public service", "accountability"],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  )
}
